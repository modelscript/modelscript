{
  "name": "modelscript-github-fs",
  "displayName": "ModelScript GitHub FileSystem",
  "version": "0.0.1",
  "private": true,
  "description": "Provides a file system provider for GitHub repositories in VS Code Web.",
  "categories": [
    "Other"
  ],
  "license": "AGPL-3.0-or-later",
  "publisher": "modelscript",
  "browser": "./dist/extension.js",
  "scripts": {
    "build": "esbuild src/extension.ts --bundle --format=cjs --platform=browser --external:vscode --outfile=dist/extension.js",
    "watch": "esbuild src/extension.ts --bundle --format=cjs --platform=browser --external:vscode --outfile=dist/extension.js --watch"
  },
  "contributes": {
    "configuration": {
      "title": "GitHub FileSystem",
      "properties": {
        "github-fs.token": {
          "type": "string",
          "default": "",
          "description": "GitHub personal access token for API authentication."
        }
      }
    },
    "resourceLabelFormatters": [
      {
        "scheme": "github",
        "formatting": {
          "label": "${path}",
          "separator": "/",
          "workspaceSuffix": "GitHub"
        }
      }
    ]
  },
  "activationEvents": [
    "*"
  ],
  "devDependencies": {
    "@types/vscode": "^1.72.0",
    "esbuild": "^0.20.0"
  },
  "engines": {
    "vscode": "^1.72.0"
  }
}
